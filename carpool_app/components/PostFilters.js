import React, { useState, useEffect } from "react";
import {
  Button,
  View,
  Text,
  FlatList,
  StyleSheet,
  TouchableOpacity,
} from "react-native";

//Popup or toggle visibility that displays filter options
function PostFilters({ setTrips }) {
  return <View></View>;
}

export default PostFilters;
